[tox]
envlist = py27,py32,py33,cover

[testenv]
deps =
		pytest
		webtest
commands = 
		py.test

[testenv:cover]
deps =
		pytest
		pytest-cov
		webtest
commands = 
		py.test --cov atramhasis --cov-report term-missing tests
